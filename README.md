# OSaC - Operating System as Code

This script is used to adjust a clean Ubuntu OS installation as much as possible according to my desires.

Use with direct download from GitHub:

```
wget -O post_install.sh <github-link> && chmod +x post_install.sh && sudo ./post_install.sh
```
